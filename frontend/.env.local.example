# ClaimPilot AI — frontend environment variables
# Copy to frontend/.env.local and adjust for your environment. Never commit .env.local.

# ── Backend API ───────────────────────────────────────────────────────────────
# Base URL of the FastAPI backend (used by all fetch/SSE calls from the browser).
NEXT_PUBLIC_API_URL=http://localhost:8000
